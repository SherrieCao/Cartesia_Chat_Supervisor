[project]
name = "taniku-izakaya-host"
version = "0.1.0"
description = "Taniku Izakaya: a bilingual (EN/JA) voice phone host for a Japanese izakaya in SF"
requires-python = ">=3.10"
dependencies = [
    "cartesia-line==0.2.15",
]

[tool.setuptools]
py-modules = ["main", "restaurant_data"]
